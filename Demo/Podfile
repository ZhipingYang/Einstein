# Uncomment the next line to define a global platform for your project

platform :ios, '9.0'
use_frameworks!

target 'Demo' do
  
  pod 'Einstein/Identifier', :path => '../'

  target 'DemoTests' do
    pod 'Einstein/Identifier', :path => '../'
  end

  target 'DemoUITests' do
    pod 'Einstein', :path => '../'
  end

end
